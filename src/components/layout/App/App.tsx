import { FC } from 'react'

import { Wrapper } from 'src/components/surfaces'
import * as s from './App.css'

const App: FC = () => {
  return <Wrapper className={s.text}>Hello!</Wrapper>
}

export default App
