import { css } from '@emotion/react'

const styles = {
  wrapper: css`
    background: var(--rgb-bg2);
    border-radius: 12px;
  `,
  padding: css`
    padding: var(--space-secondary) var(--space-primary);
  `,
}

export default styles
