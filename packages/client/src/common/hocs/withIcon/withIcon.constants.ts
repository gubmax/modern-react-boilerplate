export const DEFAULT_SIZE = '24px'

export enum IconVariants {
  PRIMARY = 'primary',
  ACCENT = 'accent',
  ACTIVE = 'active',
  SECONDARY = 'secondary',
}

export enum IconSizes {
  SMALL = 'small',
  HUGE = 'huge',
}
