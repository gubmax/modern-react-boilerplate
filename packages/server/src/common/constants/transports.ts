export enum TransportMarks {
  REQ = '$$REQ',
  REQ_INTERNAL = '$$REQ_INTERNAL',
  RES = '$$RES',
  RES_INTERNAL = '$$RES_INTERNAL',
}

export enum Transports {
  HTTP = 'http',
  // WS = 'ws',
  // REDIS = 'redis',
  // others...
}
